# Synthetic Data

Files generated in this directory are synthetic and created for academic development and testing. They are not official Rwandan land records.

Generate the dataset from the repository root:

```powershell
python -m ml.scripts.generate_dataset --output ml/data/synthetic_land_transactions.csv --rows 1200
```
